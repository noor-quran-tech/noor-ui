export interface PaginationParams {
  currentPage: number;
  totalPages: number;
  totalData: number;
  limit: number;
}

export interface TabPagination {
  currentPage: number;
  totalPages: number;
  totalData: number;
}
